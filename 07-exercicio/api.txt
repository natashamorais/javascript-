escutar pelo evento de submit do input > valor do input > usar o valor para fazer a busca > musicbrainz = http://musicbrainz.org/ws/2/artist/?query=adele&fmt=json

google key 

id mecanismo de pesquisa 

//www.googleapis.com/customsearch/v1?[parameters = cx, q e key]


?delimita o começo dos parametros e fim da url 
& diz que tem um parametro novo
>verifica se tem retorno, se retornou transforma em objeto e usa os dados para montar o html > se não retornou mensagem falando que não tem resultada> se tiver erro avisar o usuário 
